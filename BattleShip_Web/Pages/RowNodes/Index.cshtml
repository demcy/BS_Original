@page
@model BattleShip_Web.Pages_RowNodes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RowNode) {
        <tr>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RowNodeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RowNodeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RowNodeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
